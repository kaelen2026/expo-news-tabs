# Connection string to the Postgres instance from docker-compose.
DATABASE_URL=postgresql://app:app@localhost:5432/app

# Port the Hono server listens on (defaults to 3001).
PORT=3001
BETTER_AUTH_SECRET=